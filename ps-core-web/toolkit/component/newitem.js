/**
 * Created by leonlin on 16/11/3.
 */
define(['commonMethod'], function(commonMethod){
  return function(data)
  {
    var element = new commonMethod(data.element);
    var expression = data.expression;
    $$.runExpression(exp)
    var target = data.target;
    var dom = $("<div>asdasdasdasdasdsad</div>");
    if(element.style)
    {
      dom.css(element.style);
    }
    return dom;
  }
});
